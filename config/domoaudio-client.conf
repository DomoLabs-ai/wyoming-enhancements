# DomoAudio Client configuration
# Optimized for Wi-Fi connected satellites
DOMO_AUDIO_CLIENT_OPTS="--buffer_ms=300 --latency=0"
# Increase buffer_ms if experiencing underruns on Wi-Fi (default: 300)
# For wired connections, 150 is usually sufficient
