#!/bin/bash
source "$(dirname "$0")/lib/common.sh"
log_info "done signal"
require_cmd aplay
F="${DONE_SOUND:-/opt/wyoming-enhancements/sounds/done.wav}"
[ -f "${F}" ] || { log_warn "not found: ${F}"; exit 0; }
aplay -q "${F}" 2>/dev/null || log_warn "playback failed"
